defaults: &defaults
  url: http://localhost:<%= ENV['TEST_JETTY_PORT'] || 8080 %>/hydra

development:
  <<: *defaults

test:
  url: http://localhost:<%= ENV['TEST_JETTY_PORT'] || 8983 %>/solr

production:
  <<: *defaults
